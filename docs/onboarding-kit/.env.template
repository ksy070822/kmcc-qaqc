# ===================================================
# KMCC QC Dashboard - 환경변수 템플릿
# ===================================================
# 사용법: 이 파일을 .env.local로 복사한 뒤 값을 채워주세요.
#   cp .env.template .env.local
#
# 주의: .env.local은 절대 Git에 커밋하지 마세요! (.gitignore에 등록됨)
# ===================================================

# --- BigQuery 설정 ---
BIGQUERY_PROJECT_ID=csopp-25f2
BIGQUERY_DATASET_ID=KMCC_QC
GCP_LOCATION=asia-northeast3

# --- 인증 ---
# 로컬 개발: gcloud auth application-default login 실행 후 아래는 비워두세요
# Cloud Run: 서비스 계정이 자동 인증됩니다
# GOOGLE_APPLICATION_CREDENTIALS=./your-service-account-key.json

# --- Google Sheets ---
# QC 평가 데이터 원본 스프레드시트 ID (메이에게 문의)
GOOGLE_SHEETS_ID=여기에_시트_ID_입력

# --- Google AI (Gemini) - 선택사항 ---
# AI 챗봇 기능 사용 시에만 필요 (없어도 대시보드는 동작합니다)
# GOOGLE_AI_API_KEY=여기에_API_키_입력
# GOOGLE_AI_MODEL=gemini-2.5-flash

# --- Vertex AI - 프로덕션 전용 (로컬에서는 불필요) ---
USE_VERTEX_AI=false
# VERTEX_AI_PROJECT_ID=csopp-25f2
# VERTEX_AI_LOCATION=asia-northeast3
# VERTEX_AI_MODEL=gemini-2.5-flash
